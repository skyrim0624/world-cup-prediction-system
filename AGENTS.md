# 2026 世界杯动态预测系统 AGENTS

创建日期：2026-06-13  
项目目录：`/Users/andreas/vibe coding/world cup`  
知识库目录：`/Users/andreas/cmi社区知识库/CMI/世界杯预测系统打造`

## 一、项目目标

本项目要做的不是一篇静态的“世界杯夺冠热门预测”文章，而是一个可以持续更新的世界杯动态预测产品。

产品核心目标：

1. 每天根据真实比赛进展、赛程路径、球队状态和权威新闻，更新 2026 世界杯各队的夺冠概率、晋级概率和风险说明。
2. 让用户在移动端快速看懂：谁更可能夺冠，谁被高估，谁路径变难，谁因为伤病、停赛、新闻或赛程因素被下调。
3. 用稳定的底层概率模型承接专业性，用清晰的五大盘面承接用户理解，用新闻抓取模块承接每日动态。
4. 最终形态可以支持付费查看，例如单次查看、赛事全周期查看、重点场次预测包等。

本项目的判断边界：

- 这是概率分析产品，不是投注建议产品。
- 不承诺预测一定命中。
- 不使用未授权商业数据。
- 不暗示掌握内幕消息。
- 新闻抓取只基于公开来源，并且必须区分来源权威等级。

## 二、产品形态

### 1. 响应式 Web App，移动端优先

客户明确希望页面既能在电脑浏览器访问，也要方便手机浏览器访问。前端设计不能只做桌面端再缩小适配，而要从一开始按响应式 Web App 设计：桌面端展示完整数据视野，移动端提供单列滚动、触控优先、核心信息不丢失的浏览体验。

核心页面应包括：

- 单场比赛预测页。
- 今日夺冠概率榜。
- 今日概率变化榜。
- 球队详情页。
- 单队五大盘面分析。
- 新闻影响摘要。
- 小组和淘汰赛路径图。
- 付费解锁入口。

移动端必须保留的核心模块：

- 单场胜平负 / 晋级概率。
- 最可能比分和比分分布。
- 冠军概率榜。
- 五大盘面。
- 路径模拟摘要。
- 新闻影响和来源权重。
- 付费入口。
- 双击/双击触控后移动模块的提示。

视觉方向：

- 世界杯专题感。
- 绿茵场、赛程、队徽、概率热力图等体育视觉元素。
- 可参考腾讯体育、懂球帝、Opta Analyst、Football Meets Data 等产品表达，但不能复制页面设计和数据结果。

### 2. 用户看到的核心表达

用户不应该直接看到复杂公式，而应该看到能理解的解释：

```text
法国夺冠概率：13.4% → 12.1%

变化原因：
1. 核心前锋训练缺席，人员盘下调。
2. 小组第一路径仍较优，路径盘保持稳定。
3. 最近两场非点球 xG 下降，状态盘轻微下调。
```

页面要把复杂模型翻译成五大盘面：

| 盘面 | 用户理解 | 背后模型 |
|---|---|---|
| 实力盘 | 这队本来强不强 | Elo、SPI、Opta Power Rating、长期攻防强度 |
| 状态盘 | 这队最近好不好 | 最近比赛 xG、射门质量、防守质量、核心球员状态 |
| 路径盘 | 接下来这条路难不难 | 小组排名、潜在对手、半区强度、旅行消耗 |
| 人员盘 | 关键球员能不能上 | 伤病、停赛、负荷、替补可替代性 |
| 边际盘 | 细节有没有隐藏优势 | 定位球、门将、点球、天气、旅程、士气、裁判尺度 |

### 3. 用户到底在预测什么

本产品的预测对象不是单一的“谁会夺冠”，也不是单纯的“下一场谁赢”，而是：

```text
未开始比赛的单场结果走向
→ 影响小组排名和淘汰赛路径
→ 重算各队晋级与夺冠概率
→ 由 AI 生成可读的预测分析和变化原因
```

用户打开页面后，应该能获得三层预测：

| 层级 | 用户问题 | 页面输出 |
|---|---|---|
| 单场预测 | A 队对 B 队谁更可能赢 | 胜 / 平 / 负概率，淘汰赛展示双方晋级概率 |
| 比分预测 | 这场最可能踢成几比几 | 最可能比分 Top 3，比分概率分布 |
| 整届传导 | 这场结果会怎样改变世界杯走势 | 小组排名、路径难度、晋级概率、夺冠概率变化 |

比赛状态处理：

- 未开始比赛是核心预测对象。
- 进行中比赛如果没有可靠实时数据，第一版不强依赖实时修正。
- 已结束比赛不再预测，锁定真实赛果，作为后续概率和路径模拟的动态权重因子。

表达边界：

- 不把“比分概率”写成“赔率”。
- 不展示或承诺投注建议。
- 如果后续接入市场赔率，只作为市场热度或舆论偏差，不作为产品主预测。

## 三、模型总结构

本项目采用三层模型结构：

```text
底层概率模型
→ 中间事件与新闻处理层
→ 上层产品解释层
```

### 1. 底层：稳定概率模型

底层负责给出稳定、可复算的概率，不直接被新闻情绪带动。

建议底层路径：

```text
Elo / 实力评分
→ Poisson 或 Dixon-Coles 单场比分模型
→ 蒙特卡洛模拟整届世界杯
→ 输出夺冠、进决赛、进四强、进八强、出线概率
```

底层模型包含四个部分：

| 模块 | 作用 | 说明 |
|---|---|---|
| 球队强度评分 | 判断球队本身强弱 | 可参考 Elo、Opta Power Rating、SPI、FIFA 排名、最近比赛表现 |
| 单场进球模型 | 判断两队交手胜平负概率 | 可用 Poisson / Dixon-Coles，把强弱差转为比分概率 |
| 赛程路径模型 | 判断球队会遇到谁 | 根据小组排名、32 强路径、半区强度、潜在对手动态更新 |
| 蒙特卡洛模拟 | 反复模拟整届赛事 | 建议正式更新时至少模拟 50,000 次 |

底层输出：

- 小组出线概率。
- 小组第一、第二、第三概率。
- 进入 32 强、16 强、8 强、4 强、决赛、夺冠概率。
- 每场比赛胜平负概率。
- 每队路径难度。
- 每队概率变化幅度。

底层原则：

- 比赛已经结束后，必须锁定真实赛果。
- 已淘汰球队概率必须归零。
- 小组第三出线机制必须正确处理。
- 不把社媒传闻直接喂进底层公式。
- 赔率只作为市场预期参考，不等于真实概率。

### 2. 中间：事件与新闻处理层

中间层负责把比赛结果、伤病、停赛、训练、发布会、天气、旅程、队内动态等信息，转成结构化事件。

每条事件至少要包含：

| 字段 | 说明 | 示例 |
|---|---|---|
| 事件类型 | 信息类别 | 伤病、停赛、战术调整、士气、天气、赛果 |
| 影响对象 | 影响谁 | 法国队、姆巴佩、后防线、中场轮换 |
| 影响方向 | 利好、利空、中性、不确定 | 核心球员复出是利好 |
| 影响强度 | 轻微、中等、严重 | 核心前锋缺席淘汰赛是严重 |
| 来源等级 | 信息可靠性 | 官方、权威媒体、跟队记者、社媒 |
| 是否确认 | 确认状态 | 已确认、多源交叉、单源传闻、未证实 |
| 作用范围 | 是否进模型 | 直接修正、轻微修正、只做备注、等待确认 |

中间层处理三类信息：

1. 硬信息：比赛结果、积分、净胜球、红黄牌停赛、官方伤病公告。
2. 半硬信息：权威媒体伤病报道、训练缺席、主教练发布会、跟队记者消息。
3. 软信息：士气、队内气氛、更衣室传闻、社媒爆料、球迷观察。

处理原则：

- 硬信息可以直接修正概率。
- 半硬信息可以小幅修正，并等待后续确认。
- 软信息原则上只做风险提醒，不直接大幅改变概率。
- 多源交叉验证后，软信息可以升级为半硬信息。

### 3. 上层：产品解释层

上层负责把概率和事件翻译成用户能看懂的内容。

上层输出包括：

- 今日最大上调球队。
- 今日最大下调球队。
- 每队夺冠概率变化。
- 每队五大盘面评分。
- 今日关键新闻影响。
- 路径变难或变容易的原因。
- 关键球员风险提醒。
- 用户可读的模型解释。

上层不展示复杂公式，但要保留专业感。

## 四、因子重要程度

### 第一档：最高优先级，直接决定基础概率

#### 1. 球队基础实力

作用：判断球队是否具备夺冠硬实力。

可用指标：

- Elo。
- Opta Power Rating。
- SPI。
- FIFA 排名。
- 最近三年正式比赛表现。
- 对强队战绩。
- 预选赛和大赛表现。

开发处理：

- 第一版可以先建立球队基础评分表。
- 后续接入可更新数据源。
- 基础评分不应每天剧烈波动。

#### 2. 攻防质量

作用：判断球队能否稳定创造机会，同时限制对手机会。

可用指标：

- 非点球 xG / 90。
- 非点球 xGA / 90。
- xG 差值。
- xG per shot。
- 禁区内射门。
- 被对手进入禁区次数。
- 反击 xG。
- 定位球 xG。

开发处理：

- 如果第一版拿不到授权 xG 数据，可以先用公开进球、失球、射门、射正、近期战绩做替代。
- 后续有预算再接入正式 xG 或事件数据源。

#### 3. 晋级路径

作用：判断一支队实际要走的路有多难。

可用指标：

- 小组第一、第二、第三概率。
- 32 强潜在对手。
- 16 强潜在对手。
- 8 强潜在对手。
- 半区平均强度。
- 是否提前遇到顶级强队。
- 小组第三出线对应分支。

开发处理：

- 路径模型必须从第一版就做。
- 蒙特卡洛模拟要输出每轮概率。
- 每场比赛结束后路径必须重算。

### 第二档：高优先级，影响短期概率和淘汰赛上限

#### 4. 阵容健康与可用性

作用：判断关键球员能否连续出场。

可用指标：

- 官方伤病。
- 训练缺席。
- 过去 60 天出场分钟。
- 赛季累计负荷。
- 红黄牌停赛。
- 替补能否替代同一功能。

开发处理：

- 第一版可以手动录入关键伤病和停赛。
- 新闻抓取模块上线后，自动识别伤病和停赛事件。

#### 5. 主教练水平与临场调整

作用：判断球队是否能在淘汰赛高压下正确应对。

可用指标：

- 教练任期长度。
- 大赛淘汰赛经验。
- 换人后球队表现。
- 落后时追平能力。
- 领先后保护优势能力。
- 是否有 Plan B。

开发处理：

- 第一版不强行公式化。
- 先作为人工评分和备注因子。
- 后续可以根据历史比赛数据拆成量化项。

#### 6. 关键人物影响

作用：判断球星或核心节点对球队系统的影响。

可用指标：

- 关键球员 shot share。
- chance creation share。
- progressive carries。
- xA。
- 传球网络中心度。
- 核心缺阵时球队表现。
- 点球手稳定性。

开发处理：

- 第一版先维护每队 2-3 名关键人物。
- 新闻和伤病事件优先匹配关键人物。
- 关键人物事件的权重要高于普通替补事件。

### 第三档：中优先级，影响强队互打和淘汰赛细节

#### 7. 战术适配与风格克制

作用：判断不同风格对手之间是否存在克制关系。

可用指标：

- 控球压制能力。
- 高位压迫能力。
- 低位防守能力。
- 反击效率。
- 破密集防守能力。
- 是否怕高压。
- 是否怕反击。

开发处理：

- 第一版先用人工标签。
- 后续接入战术数据后再量化。
- 页面展示时可以转成“怕高压”“反击强”“定位球强”等标签。

#### 8. 凝聚力与阵容稳定

作用：判断球队内部是否能稳定执行计划。

可用指标：

- 主力阵容稳定度。
- 中卫搭档合作场次。
- 中场组合合作场次。
- 俱乐部队友块。
- 队长和核心是否清晰。
- 是否存在公开用人争议。

开发处理：

- 第一版作为人工备注。
- 新闻模块只在多源确认时影响概率。

#### 9. 定位球、门将与点球

作用：处理淘汰赛高杠杆细节。

可用指标：

- 定位球 xG。
- 定位球 xGA。
- 角球第一点成功率。
- 空中对抗能力。
- 门将扑救表现。
- 门将出球稳定性。
- 点球手质量。
- 点球大战经验。

开发处理：

- 第一版可以作为边际盘评分。
- 淘汰赛阶段提高该模块展示权重。

### 第四档：边际因素，强队接近时才放大

#### 10. 旅行、气候与恢复

作用：解释强队接近时的边际变化。

可用指标：

- 小组赛总飞行距离。
- 跨时区次数。
- 高温高湿场次。
- 高海拔场次。
- 比赛间隔天数。
- 训练基地到赛场距离。

开发处理：

- 第一版可以先基于赛程和城市手动计算。
- 后续接入城市坐标自动计算旅行距离。

#### 11. 市场与舆论偏差

作用：观察大众预期，不直接决定球队强弱。

可用指标：

- 博彩赔率变化。
- 预测市场价格。
- 媒体讨论量。
- 社交平台情绪。
- 热门队是否被过度追捧。

开发处理：

- 第一版可以暂不接赔率。
- 如接入赔率或预测市场，必须确认数据授权。
- 页面上只作为“市场热度”展示，不作为投注建议。

## 五、新闻抓取系统

### 1. 新闻模块定位

新闻模块不是用来替代模型，而是用来发现短期变化。

它负责：

1. 抓取公开新闻、官方公告、权威媒体、跟队记者消息。
2. 提取事件：伤病、停赛、训练、发布会、士气、天气、旅程、队内动态。
3. 判断来源权威等级。
4. 判断事件对哪支球队、哪名球员、哪个盘面有影响。
5. 给概率变化生成解释。

### 2. 新闻来源权威等级

| 等级 | 来源 | 是否进模型 | 处理方式 |
|---|---|---|---|
| S 级 | FIFA、世界杯官方、国家队官方、国家足协、官方首发、官方伤病、官方红黄牌 | 直接进入 | 权重最高，和低等级冲突时以 S 级为准 |
| A 级 | Opta、StatsBomb、The Athletic、BBC、ESPN、Sky Sports、L'Équipe、Reuters、AP、Guardian | 可以进入 | 重大事件建议多源交叉，伤病可中幅修正 |
| B 级 | 可靠跟队记者、本地权威记者、长期准确的实名记者 | 谨慎进入 | 单源轻微修正，多源后可提高权重 |
| C 级 | 普通媒体、自媒体、球迷社区、YouTube 分析、未知准确率 X 账号 | 不直接进入 | 只作为待确认线索 |
| D 级 | 匿名爆料、群聊截图、无出处搬运、“内部消息” | 不进入 | 默认忽略或标注未证实 |

### 3. 新闻事件权重

| 事件类型 | 影响强度 | 是否进入概率 | 示例 |
|---|---|---|---|
| 比赛结果 | 极高 | 直接进入 | 小组积分、净胜球、路径变化 |
| 红黄牌停赛 | 高 | 直接进入 | 主力中卫累计黄牌停赛 |
| 官方伤病公告 | 高 | 直接进入 | 核心前锋确认缺席 |
| 官方首发名单 | 高 | 进入单场模型 | 比赛前一小时更新胜率 |
| 权威伤病报道 | 中高 | 小幅进入 | 多家媒体称核心球员伤情未明 |
| 训练缺席 | 中 | 轻微修正或备注 | 连续两天缺席训练 |
| 主教练发布会 | 中 | 视内容进入 | 暗示轮换或确认伤情 |
| 战术调整报道 | 中 | 备注或小幅进入 | 权威分析称改打三中卫 |
| 更衣室矛盾 | 中低 | 原则上备注 | 多源确认后轻微影响凝聚力 |
| 士气变化 | 中低 | 原则上备注 | 队长发言、赛后情绪 |
| 天气与场地 | 低到中 | 小幅进入 | 高温高湿、高海拔、草皮问题 |
| 旅程与恢复 | 低到中 | 小幅进入 | 连续跨时区远距离飞行 |
| 社媒爆料 | 低 | 不直接进入 | 未证实传闻 |

### 4. 新闻处理流程

每日流程：

1. 抓取官方赛程、比分、积分、红黄牌。
2. 锁定已结束比赛结果。
3. 更新小组排名和路径概率。
4. 抓取官方伤病、发布会、训练和首发信息。
5. 抓取权威媒体与跟队记者新闻。
6. 把新闻转成结构化事件。
7. 对事件做来源等级判断。
8. 判断事件是否进入模型。
9. 重新跑概率模拟。
10. 生成面向用户的变化原因。

赛前流程：

1. 比赛前 24 小时更新天气、训练、伤病、停赛。
2. 比赛前 1 小时读取官方首发。
3. 更新单场胜平负概率。
4. 更新整届赛事蒙特卡洛模拟。
5. 输出赛前风险提示。

赛后流程：

1. 锁定比分。
2. 更新积分和路径。
3. 记录红黄牌、伤病、争议事件。
4. 读取比赛过程数据。
5. 更新状态盘。
6. 输出赛后概率变化说明。

## 六、算法与许可证边界

### 1. 可采用的方法

| 方法 | 是否可用 | 说明 |
|---|---|---|
| 蒙特卡洛模拟 | 可以 | 公开数学方法，可自己实现 |
| Poisson 模型 | 可以 | 公开统计方法，可自己实现 |
| Dixon-Coles | 可以 | 1997 年公开论文模型，可按公式实现 |
| Elo | 可以 | 常见评分方法，可自己实现 |

### 2. 可参考的项目

| 项目 | License / 属性 | 建议 |
|---|---|---|
| Cup26 AI / world-cup-2026-prediction-model | MIT | 可参考结构，谨慎复用代码，不复制页面和结果 |
| penaltyblog | MIT | 可评估作为 Python 建模库 |
| regista | GPL-3.0 | 不建议直接引入闭源商用项目 |
| Football Meets Data | 商业产品 | 只参考表达和方法，不复制数据和模拟器 |

### 3. 数据授权原则

算法可以自己实现，不等于数据可以随便使用。

相对安全：

- 官方赛程。
- 官方比分。
- 官方积分。
- 官方红黄牌。
- 官方首发。
- 官方发布会摘要。

高风险：

- Opta 详细事件数据。
- StatsBomb 付费数据。
- 商业赔率 API。
- 付费媒体全文。
- Football Meets Data 的模拟结果。
- 第三方平台专有概率。

第一版建议：

- 使用公开赛程和比分。
- 自建球队基础评分。
- 手动或半自动录入伤病、停赛和新闻事件。
- 不直接使用未授权商业数据。

## 七、开发计划

### 阶段 0：文档与客户确认

目标：先把系统定义清楚，避免后续扯皮。

产物：

- `AGENTS.md`：项目入口和开发日志。
- `世界杯动态预测系统-模型结构与新闻因子设计.md`。
- `世界杯预测模型权重因子：.md`。
- `世界杯预测模型-开源算法与许可证调研.md`。

需要客户确认：

- 是否接受“概率分析，非投注建议”的定位。
- 是否接受第一版先用公开数据和自建模型。
- 付费模式：单次、全赛事、场次包。
- 是否需要微信/支付宝支付。
- 是否需要后台手动录入新闻事件。

### 阶段 1：前端原型

目标：先做响应式 Web App 页面样子，桌面浏览器和手机浏览器同时成立。

页面：

- 单场预测页。
- 首页概率榜。
- 球队详情页。
- 今日变化页。
- 新闻影响页。
- 付费解锁弹层。

原则：

- 先用 mock 数据。
- 首页需要清楚表达“单场预测如何传导到整届概率变化”。
- 单场页至少展示胜平负概率、最可能比分、路径影响和 AI 分析摘要。
- 移动端优先，但不能牺牲桌面端的数据视野。
- 手机端使用单列滚动、触控优先、底部/顶部固定关键入口。
- 桌面端使用多栏数据看板，承载更多路径和模型细节。
- 不做多余按钮和无关功能。
- 页面信息密度要适中，不能像论文。

### 阶段 2：底层模拟引擎

目标：让概率真的能跑起来。

功能：

- 球队基础评分表。
- 小组赛和淘汰赛赛程结构。
- 单场胜平负概率。
- 单场比分概率分布。
- 蒙特卡洛模拟。
- 每轮晋级概率输出。
- 单场结果对整届概率的传导输出。
- 已结束比赛锁定。

### 阶段 3：事件与新闻系统

目标：让概率变化有解释。

功能：

- 新闻/事件数据结构。
- 来源等级评分。
- 事件影响方向。
- 事件影响强度。
- 人员盘、路径盘、状态盘等盘面映射。
- 每日变化原因生成。

### 阶段 4：后台与付费

目标：让产品可运营。

功能：

- 后台录入或审核新闻事件。
- 概率更新任务。
- 付费解锁。
- 支付宝/微信支付接口。
- 用户权限。
- 访问记录。

### 阶段 5：上线与日常运营

目标：每天稳定更新。

日常动作：

- 每天更新比分和路径。
- 每天更新关键新闻。
- 每天生成概率变化说明。
- 大赛节点做深度报告。
- 赛后复盘模型偏差。

## 八、后续文档规则

后续每完成一个重要步骤，都要留下文档，方便交接给客户。

建议文档结构：

```text
世界杯预测系统打造/
├── AGENTS.md
├── 世界杯动态预测系统-模型结构与新闻因子设计.md
├── 世界杯预测模型权重因子：.md
├── 世界杯预测模型-开源算法与许可证调研.md
├── 前端界面设计记录.md
├── 数据结构设计记录.md
├── 模拟引擎开发记录.md
├── 新闻抓取系统开发记录.md
├── 支付系统开发记录.md
└── 客户确认记录.md
```

每份开发记录至少包含：

- 日期。
- 本次目标。
- 已完成内容。
- 关键决策。
- 未解决问题。
- 下一步。

## 九、本次开发日志

### 2026-06-13：项目需求与模型方向确定

已完成：

- 阅读客户提供的 Word 文档。
- 梳理客户聊天截图中的真实需求。
- 判断客户要的是移动端动态预测页面，而不是静态预测文章。
- 初步确认产品需要收费查看能力。
- 梳理模型需要采用三层结构：底层概率、中间事件、上层解释。
- 调研专业足球预测团队和分析师常用因子。
- 形成五大盘面：实力盘、状态盘、路径盘、人员盘、边际盘。
- 调研蒙特卡洛、Poisson、Dixon-Coles、Cup26 AI、penaltyblog、regista、Football Meets Data 的可用性和许可证边界。
- 确认新闻抓取模块应该作为事件和解释层，不应该让传闻直接接管模型。

关键决策：

- 底层概率用 Elo / Poisson 或 Dixon-Coles / 蒙特卡洛。
- 产品解释用五大盘面。
- 新闻系统必须做来源等级。
- 官方新闻和比赛结果可以直接进模型。
- 社媒传闻不能直接进模型。
- 第一版先用公开数据和自建模型，不直接使用未授权商业数据。

下一步：

- 设计响应式前端界面，包含桌面端和手机浏览器端。
- 先用 mock 数据做页面原型。
- 前端确认后，再进入模拟引擎和数据结构开发。

### 2026-06-14：预测对象与产品入口收敛

已完成：

- 明确用户打开网页后不是只看“夺冠热门”，也不是只看“下一场谁赢”，而是看未开始比赛如何改变整届世界杯走势。
- 确认核心入口应包括单场比赛预测：胜 / 平 / 负概率，淘汰赛双方晋级概率，最可能比分和比分概率分布。
- 确认整届预测仍然重要，但它应作为单场比赛结果传导后的上层结果：小组排名、路径难度、晋级概率、夺冠概率变化。
- 确认已结束比赛不再作为预测对象，而是锁定为真实赛果，进入后续路径和权重更新。
- 确认“几比几的赔率”在产品里应改写为“比分概率”或“最可能比分”，避免把产品推向投注建议语境。

关键决策：

- 产品定义更新为：以未开赛比赛预测为入口，以整届世界杯概率变化为结果的动态预测系统。
- 第一版前端需要补足“单场预测 → 路径模拟 → 晋级 / 夺冠概率变化”的用户阅读链路。
- 第一版模型需要在已有胜平负概率之外，增加比分概率分布和单场结果传导输出。
- 进行中比赛的实时修正不是第一版刚需，除非后续有可靠实时数据源。

下一步：

- 基于现有 Forecast Console 原型，重新整理信息架构，不马上写代码。
- 提出前端模块调整建议：把当前实时比分终端改成未开赛单场预测入口，并补充整届概率传导模块。
- 提出数据结构调整建议：增加 match prediction、score distribution、tournament impact 三类数据。
- 再进入实现前，需要确认首页优先展示“单场预测”还是“今日关键比赛 + 冠军概率榜”的组合。

### 2026-06-14：前端原型调整为未开赛预测入口

已完成：

- 将现有 Forecast Console 原型从实时比分终端调整为未开赛比赛预测终端。
- 顶部改为 `BRA VS ARG` 对阵卡，展示比赛阶段、开赛时间、预测更新时间和未开赛状态。
- 将核心模块改为赛前胜平负概率。
- 新增最可能比分模块，使用比分概率表述，不使用“赔率”。
- 新增整届概率传导模块，展示不同赛果对小组排名、路径和夺冠概率的影响。
- 新增冠军概率榜，补上单场预测向整届预测传导后的结果视图。
- 保留新闻影响、来源权重、三层模型、五大盘面、预测权重因子和双击移动模块。

验证：

- `npm run build` 通过。
- 桌面 1280px 和手机 390px 自动化检查无横向溢出。
- 页面核心模块存在：赛前胜平负概率、最可能比分、整届概率传导、冠军概率榜。
- 页面文案未出现“赔率”。
- 选择球队和双击解锁模块交互通过。

下一步：

- 模型层优先定义 `matchPrediction`、`scoreDistribution`、`scenarioImpacts`、`tournamentProbabilities` 四类数据结构。
- 前端层下一步应补单场详情页，避免继续在首页堆太多模块。

### 2026-06-14：进入真模型 MVP 补全

本轮判断：

- 当前版本已经达到动态预测产品的前端表达，但还没有达到客户最初要求的动态预测模型。
- 关键缺口不是视觉，而是真实模型、真实接口、赛果锁定、路径重算和事件修正。

按重要性排序的补全计划：

1. 建立最小球队评分、赛程和事件数据层。
2. 实现 Poisson 单场比分概率，输出胜 / 平 / 负和最可能比分。
3. 实现蒙特卡洛整届路径模拟，输出冠军、决赛、四强、八强概率。
4. 实现已结束比赛锁定，让真实赛果进入后续预测。
5. 实现事件修正 MVP，让伤病、停赛和来源等级影响球队强度，但社媒传闻不入模型。
6. 暴露 `/api/match-prediction` 接口，让前端不再只依赖 mock。
7. 前端读取真实接口返回的冠军榜和事件解释。

边界：

- 第一版模型使用自建公开参数，不接未授权商业数据。
- 第一版新闻事件先用本地结构化样例，不做自动抓取。
- 第一版蒙特卡洛模拟次数以开发可验证为主，先用 8,000 次，后续再提高到 50,000 次。

### 2026-06-14：真模型 MVP 第一版完成

已完成：

- 新增 Python / FastAPI 后端。
- 新增本地球队评分、赛程和事件数据。
- 实现 Poisson 单场比分概率，输出胜 / 平 / 负概率和最可能比分。
- 实现蒙特卡洛路径模拟，输出冠军、决赛、四强、八强概率。
- 实现已结束比赛锁定，当前样例锁定 3 场赛果。
- 实现事件来源权重，D 级社媒传闻不入模型。
- 暴露 `/api/match-prediction` 接口。
- Vite 开发服务器代理 `/api` 到 FastAPI。
- 前端读取真实接口返回的球队概率、模拟次数和锁定赛果数量。
- 新增模型测试脚本 `npm run test:model`。

验证：

- `npm run test:model` 通过。
- `npm run build` 通过。
- 前后端联调通过，页面显示 `真实模型`、`8,000 次模拟`、`已锁定 3 场赛果`。
- 桌面 1280px 和手机 390px 自动化检查无横向溢出。
- 页面文案未出现“赔率”。

当前仍未完成：

- 还没有真实 2026 世界杯完整赛程和 48 队结构。
- 新闻事件还是本地样例，没有自动抓取和多源交叉验证。
- 模型参数是自建 MVP 参数，不是商业数据源。
- 没有后台录入页面。
- 没有支付和用户权限。

下一步：

- 扩展到完整赛程数据结构。
- 把本地事件数据改成可后台录入或 JSON 文件维护。
- 把模拟次数从 8,000 提高到正式更新用的 50,000。
- 再补单场详情页和每日变化页。

### 2026-06-14：模型可维护性与覆盖范围增强

已完成：

- 将球队、赛程、事件、来源权重和当前比赛从 Python 常量迁移到 `backend/data_files/*.json`。
- 新增数据加载和基础校验：重复球队、未知球队、已完赛缺比分都会报错。
- 球队池从 4 队扩展到 8 队，新增英格兰、葡萄牙、德国、荷兰。
- 赛程从单组扩展到 E/F 两组样例。
- 修正小组排名逻辑：每个小组只按本组球队排名，不再把所有球队混在同一组里排序。
- 蒙特卡洛路径模拟改为先按组出线，再进行跨组淘汰路径模拟。
- `/api/match-prediction` 增加模型透明度：数据集来源、事件观察数、入模数、忽略数。
- 新增 `/api/model-status`，暴露当前模型覆盖范围和已知缺口。
- 前端模型说明行展示事件入模/忽略统计。
- 前端支持 8 队列表和新增球队旗帜。

验证：

- `npm run test:model` 通过，覆盖 7 个模型/API 测试。
- `npm run build` 通过。
- 前后端联调通过，页面显示 `真实模型`、`事件 1 入模 / 1 忽略` 和 8 队数据。
- 桌面 1280px 与手机 390px 自动化检查无横向溢出。
- 页面文案未出现“赔率”。

当前仍未完成：

- 仍是 8 队样例，不是完整 48 队。
- 淘汰赛路径仍是 MVP 结构，不是 2026 真实 32 强路径。
- 事件仍为本地 JSON，没有后台录入和自动抓取。
- 模拟次数仍为 8,000，正式更新应提升到 50,000。

### 2026-06-14：模拟次数配置化

已完成：

- `/api/match-prediction` 支持 `simulations` 参数。
- 参数范围限制为 1,000 到 50,000，避免误请求拖垮接口。
- 默认仍为 8,000 次，适合开发和交互预览。
- 正式更新可以请求 `?simulations=50000`。

验证：

- `npm run test:model` 通过，覆盖接口参数测试。
- `npm run build` 通过。
- 本地直接运行 50,000 次模拟成功返回。

当前判断：

- 模型已经具备“开发快跑 / 正式高精度更新”的基本运行方式。
- 下一步应继续补真实 48 队赛程结构和事件维护能力。

### 2026-06-14：每日预测快照生成

已完成：

- 新增 `backend/snapshot.py`，可把当前预测结果写成 JSON 快照。
- 新增 `scripts/update_prediction_snapshot.py` 命令入口。
- 新增 `npm run update:snapshot`。
- 默认快照路径为 `backend/snapshots/latest-match-prediction.json`。
- 快照文件加入 `.gitignore`，避免每日预测结果污染版本历史。
- 快照包含 `snapshotMeta`，记录类型、生成时间和输出路径。

验证：

- `npm run test:model` 通过，覆盖 10 个模型/API/快照测试。
- `npm run build` 通过。
- `python3 scripts/update_prediction_snapshot.py --simulations 1200 --output /tmp/world-cup-latest.json` 成功生成快照。

当前判断：

- 产品已经具备“接口实时算”和“每日预计算快照”两种模式的基础。
- 后续完整赛程和新闻抓取接入后，可以把快照命令放入定时任务。

### 2026-06-14：48 队赛制骨架与 32 强路径

已完成：

- 按 2026 世界杯结构补齐 48 个参赛槽位。
- 数据层自动生成 12 个小组，每组 4 队。
- 自动补齐小组赛轮转，形成 72 场小组赛结构。
- 新增最佳小组第三出线逻辑：12 个第三名中选 8 个。
- 蒙特卡洛路径从两组样例升级为 32 强路径：32 强、16 强、8 强、4 强、决赛、冠军。
- 新增模型测试：48 队、12 组、每组 4 队、8 个最佳第三、32 强路径阶段。
- 优化比分采样：将比分分布预编译为累计采样器。

验证：

- `npm run test:model` 通过，当前 13 个测试，耗时约 6 秒。
- `npm run build` 通过。
- 1,200 次完整预测从约 7 秒优化到约 2.5 秒。

当前仍未完成：

- 48 队里大部分仍是可替换槽位，不是假装官方名单。
- 32 强配对仍是 MVP 排序路径，不是官方完整对阵映射。
- 还没做小组第三对应分支的官方路径表。

### 2026-06-14：事件因子接入五大盘面与性能修正

已完成：

- 新增事件因子影响明细：按来源权重输出每队 attack、defense、goalkeeper、path、squad 的正负修正。
- 模型先汇总事件影响，再统一修正球队底层评分，避免事件逻辑散落在多个地方。
- 后端返回 `modelMeta.factorImpacts`，让前端能解释“哪个盘面被调高或调低”。
- 球队输出从底层攻防指标改为产品层五大盘面：实力盘、状态盘、路径盘、人员盘、边际盘。
- 前端取消 8 队 key 限制，支持 48 队和占位槽位。
- 占位球队显示队码 fallback，冠军概率榜可完整渲染 48 队。
- 五大盘面 UI 显示事件修正值，例如状态盘受伤病新闻影响下调。
- 前端交互请求改为 `simulations=1200`，刷新间隔改为 15 秒，避免请求堆积。
- `npm run dev:api` 去掉 `uvicorn --reload`，解决本地接口从 1 秒级被拖慢到 8-10 秒的问题。

验证：

- `npm run test:model` 通过，15 个测试。
- `npm run build` 通过。
- 稳定 API 模式下，`/api/match-prediction?simulations=1200` 经 Vite 代理约 0.75 秒返回。
- 浏览器自动化检查通过：桌面 1280px 无页面溢出，手机 390px 页面宽度等于视口，页面进入 `真实模型`，48 队冠军榜渲染完整。

当前判断：

- 当前版本已经达到“动态预测模型 MVP”的核心闭环：单场胜平负、比分概率、已完赛锁定、路径传导、48 队赛制骨架、事件权重、五大盘面解释和前端真实模型展示。
- 还没有达到最终客户可运营版本，因为官方 32 强路径映射、真实名单/赛程导入、新闻自动抓取、后台录入审核、付费和权限仍未完成。

### 2026-06-14：官方淘汰赛路径与接口短缓存

已完成：

- 参考 FIFA World Cup 26 Regulations 2026 年 5 月版 Article 12.6-12.11，接入官方固定淘汰赛路径。
- 32 强不再按排名排序后相邻配对，改为 M73-M88 固定 match number。
- 16 强、8 强、4 强和决赛按 M89-M96、M97-M100、M101-M102 逐轮推进。
- 新增 `build_round_of_32_matches`，用小组第一、第二和最佳第三名生成 32 强对阵。
- 新增第三名槽位约束匹配，按官方每个第三名槽位允许的小组集合分配最佳第三名。
- 修复最佳第三名重复排名 bug：最佳第三名复用同一次小组排名，避免同队同时进入第二名和第三名槽位。
- 新增 15 秒 API 短缓存，同一 `simulations` 参数在一个刷新窗口内复用结果。
- 前端开发环境直连 `127.0.0.1:8000`，生产环境仍使用相对 `/api`。

验证：

- `npm run test:model` 通过，17 个测试。
- `npm run build` 通过。
- 直连 API 首次 `simulations=1200` 约 0.9 秒，缓存命中约 0.013 秒。
- 浏览器自动化检查通过：桌面/手机均进入 `真实模型`，48 队冠军榜完整，五大盘面完整，页面无横向滚动。

当前仍未完成：

- 真实名单、真实分组、真实赛程导入、新闻自动抓取、后台审核和付费权限仍未完成。

### 2026-06-14：Annex C 第三名组合表数据化

已完成：

- 从 FIFA World Cup 26 Regulations 2026 年 5 月版 PDF 抽取 Annexe C 表格。
- 新增 `backend/data_files/third-place-combinations.json`，包含 495 种第三名组合。
- 组合表 key 为晋级第三名小组集合，value 为 1A / 1B / 1D / 1E / 1G / 1I / 1K / 1L 对应的第三名小组。
- `backend/data.py` 加载并校验组合数必须为 495。
- `assign_third_place_slots` 优先查 Annex C 表，只有数据缺失时才退回约束匹配。
- 新增测试验证 495 表加载和已知组合 `EFGHIJKL` 映射。

验证：

- `npm run test:model` 通过，18 个测试。
- `npm run build` 通过。

当前判断：

- 路径模型的最大结构性缺口已补上：32 强固定路径、16 强后续路径和 495 种第三名组合都已按 FIFA Regulations 接入。
- 下一步应转向真实名单/赛程导入、新闻事件自动化、后台审核和付费权限。

### 2026-06-14：正式接口快照优先

已完成：

- 新增 `read_prediction_snapshot`。
- `/api/match-prediction` 默认优先读取 `backend/snapshots/latest-match-prediction.json`。
- 如果快照不存在，接口自动回退现场计算。
- 如需强制现场计算，可传 `useSnapshot=false`。
- 补充快照读取测试。

验证：

- `npm run test:model` 通过，20 个测试。
- `npm run build` 通过。

当前判断：

- 正式站已具备“后台定时生成快照、前台优先读快照、缺快照再现场计算”的基础架构。
- 后续可把 `npm run update:snapshot` 接入定时任务或后台运营按钮。

### 2026-06-14：预测数据校验命令

已完成：

- 新增 `scripts/validate_prediction_data.py`。
- 新增 `npm run validate:data`。
- 校验 48 队、12 组、每组 4 队、至少 72 场小组赛、495 个 Annex C 组合、当前比赛引用和事件引用。
- 新增数据校验测试。

验证：

- `npm run validate:data` 通过。
- `npm run test:model` 通过，21 个测试。
- `npm run build` 通过。

当前判断：

- 真实数据导入前已有最低安全线。
- 后续替换真实 48 队名单和赛程时，必须先跑 `npm run validate:data`，再跑模型测试和构建。

### 2026-06-14：新闻原始数据到事件审核管线

已完成：

- 新增 `backend/data_files/news-sources.json`，维护新闻来源和 S/A/B/C/D 等级。
- 新增 `backend/data_files/raw-news.json`，作为后续抓取器或人工录入的原始新闻输入。
- 新增 `NewsSource`、`RawNewsItem` 数据结构。
- 原始新闻会被推断为事件因子、影响方向和影响强度。
- 新增事件动作：`apply` 入模、`watch` 待审核、`ignore` 忽略。
- 模型只让 `action=apply`、来源权重大于 0、且绑定具体球队的事件进入概率修正。
- `/api/events` 暴露事件列表、来源等级、审核动作和摘要。
- `event_summary` 增加 `reviewRequired`。
- 没有绑定具体球队的事件展示为“全局备注”，不再误写成“可入模型”。
- `npm run validate:data` 增加新闻来源和原始新闻统计校验。

验证：

- `npm run validate:data` 通过。
- `npm run test:model` 通过，22 个测试。
- `npm run build` 通过。
- `/api/events` 返回摘要：7 条事件，3 条入模，2 条忽略，1 条待审核，4 条原始新闻。

当前判断：

- 新闻层已经从静态样例推进到可运营的数据流雏形。
- 后续外部抓取任务只需要写入 `raw-news.json` 或同结构数据源，再由后台审核决定是否入模。

### 2026-06-14：新闻审核写入命令

已完成：

- 新增 `backend/event_review.py`。
- 新增 `review_raw_news_item`，可更新指定 raw news 的 `status` 和 `team`。
- 新增 `scripts/review_news_item.py`。
- 新增 `npm run review:news`。
- 新增审核写入测试。

验证：

- 使用临时 raw-news 文件执行 `npm run review:news -- --id mexico-heat-watch --status confirmed --team brazil` 成功。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，23 个测试。
- `npm run build` 通过。

当前判断：

- 后台审核页面还未实现，但审核动作已有可复用后端函数和 CLI。
- 后续后台 UI 应调用同一套逻辑，而不是另写一套审核规则。

### 2026-06-14：新闻审核 API 与前端运营面板

已完成：

- `TeamEvent` 增加 raw news `id`，`/api/events` 返回事件唯一标识。
- 新增 `POST /api/events/review`，复用 `review_raw_news_item` 写入 raw-news 审核状态和绑定球队。
- CORS 增加 POST 支持。
- `model-status` 明确当前限制：审核写入后需要重建快照或重启进程，运行中模型暂不热重载。
- C 级来源在 `confirmed` 或 `multi_source` 后可从 `watch` 升级为 `apply`，以低权重进入模型流。
- 前端新增“事件审核”模块，展示原始新闻、入模、待审、忽略数量。
- 前端待审核项支持“多源确认”和“忽略”，写入成功后本地更新队列状态并提示重建快照。

验证：

- `npm run validate:data` 通过。
- `npm run test:model` 通过，26 个测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：1280px 与 390px 下审核面板无横向溢出，待审核按钮渲染正常。

当前判断：

- 新闻事件从“原始输入 → 来源评级 → 待审 → 人工确认/忽略”的运营闭环已成立。
- 最大剩余缺口变成审核后的热重载与快照触发：审核结果已经写入 raw-news，但正式预测仍需要重建快照或重启 API 后完全反映。

### 2026-06-14：审核后快照重建闭环

已完成：

- 数据层新增 `RuntimeDataset`，可按指定 raw-news 文件重新构建球队、赛程、来源、原始新闻、事件和元信息。
- 新增 `reload_runtime_data`，用于在运行中刷新数据层全局状态。
- 模型层新增 `reload_model_data`，同步刷新模型使用的球队、赛程、事件、来源权重、当前比赛和 Annex C 数据。
- 新增 `POST /api/snapshot/rebuild`，先按当前 raw-news 路径 reload 模型，再写入预测快照。
- `/api/match-prediction` 改为读取可配置的 `snapshot_data_path`。
- 前端“事件审核”模块新增“重建快照”按钮，成功后直接用新快照刷新预测页面。
- `model-status` 删除“运行中模型暂不热重载”的过时限制。

验证：

- 新增 API 测试：审核临时 raw-news 后立即重建快照，断言新新闻以“轻微修正”进入快照输出。
- `npm run test:model` 通过，27 个测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：1280px 和 390px 下“重建快照”按钮无横向溢出。

当前判断：

- 新闻事件运营链路现在闭合为：raw-news 输入 → 网页审核 → 多源确认/忽略 → 重建快照 → 前台读取新快照。
- 下一步应补外部新闻抓取和后台录入，而不是继续扩前端展示。

### 2026-06-14：原始新闻录入 API

已完成：

- 新增 `backend/news_ingest.py`。
- 新增 `append_raw_news_item`，支持向 raw-news JSON 追加新闻线索，并校验状态和重复 id。
- 新增 `POST /api/raw-news`。
- 接口会校验新闻来源是否存在、球队 key 是否有效。
- 录入后立即 `reload_model_data`，让 `/api/events` 不重启即可看到新增线索。
- 录入只进入审核队列，不绕过来源评级、人工审核和快照重建流程。

验证：

- 新增 API 测试：向临时 raw-news 追加新闻后，文件写入成功，`/api/events` 立即返回新增事件。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，8 个 API 测试。

当前判断：

- 后台录入的底层接口已经有了。
- 前端暂时不扩复杂表单，避免首页继续膨胀；后续可以在单独后台页或外部抓取任务里调用同一个接口。

### 2026-06-14：赛果锁定 API 与审核队列即时刷新

已完成：

- 数据层 `load_fixtures` 支持按指定 fixtures 文件重载。
- `load_runtime_dataset` 和 `reload_runtime_data` 增加 fixtures 路径参数。
- `reload_model_data` 同步刷新赛程数据，模型可以在同一进程内吃到新赛果。
- 新增 `backend/fixture_update.py`。
- 新增 `record_fixture_result`，可把指定比赛写成 `finished` 并记录 `home_score` / `away_score`。
- 新增 `POST /api/fixtures/result`。
- 赛果写入后立即 reload 模型和清理预测缓存。
- `POST /api/events/review` 写入审核结果后也会立即 reload，让 `/api/events` 不再依赖前端本地假更新。

验证：

- 新增 API 测试：用临时 fixtures 文件写入巴西 2-1 阿根廷后，文件状态变为 finished，`/api/model-status` 锁定赛果数变为 1。
- 增强审核 API 测试：审核临时 raw-news 后，`/api/events` 立即返回更新后的 status 和 team。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，9 个 API 测试。

当前判断：

- 动态预测模型的两条基础运营输入已经闭合：新闻事件可以录入/审核，已结束比赛可以锁定真实赛果。
- 下一步应该把这些能力组织到独立后台页，并接外部赛程/新闻抓取任务。

### 2026-06-14：未开赛比赛预测列表

已完成：

- 新增 `build_upcoming_match_predictions`。
- 新增 `GET /api/upcoming-matches`，可按 `limit` 返回未开赛比赛预测列表。
- 每场输出阶段、开赛时间、双方 key/name/code、胜平负概率和最可能比分。
- 胜平负百分比在输出层归一为 100，避免前端展示因四舍五入出现 99/101。
- 前端新增“未开赛预测”模块，默认展示 6 场未开赛比赛。
- 移动端下改为单列卡片，避免概率按钮和球队名挤压。

验证：

- 新增 API 测试：`/api/upcoming-matches?limit=5` 返回 5 场 scheduled 比赛，且胜平负概率合计 100。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，10 个 API 测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：1280px 和 390px 下“未开赛预测”模块无横向溢出，6 场比赛渲染正常。

当前判断：

- 首页现在同时具备“当前重点单场预测”和“多场未开赛预测列表”。
- 下一步更适合做单场详情页，把每场的比分分布、路径传导和 AI 分析拆出去，而不是继续向首页堆模块。

### 2026-06-14：任意未开赛单场详情

已完成：

- `simulate_tournament` 支持指定 `forced_match`，不再只能强制当前比赛场景。
- `build_scenario_impacts` 对任意传入的 home/away 比赛计算三种赛果的整届概率传导。
- 新增 `score_outcomes_for_match`，复用比分 Top 3 输出逻辑。
- 新增 `build_match_detail`。
- 新增 `GET /api/match-detail`，支持 `home`、`away` 和 `simulations` 参数。
- 前端“未开赛预测”列表可点击选择比赛。
- 前端新增“单场详情”模块，展示胜平负、比分 Top 3 和整届概率传导。

验证：

- 新增 API 测试：`/api/match-detail?home=spain&away=argentina&simulations=1200` 返回对应比赛详情，比分和传导各 3 项，胜平负合计 100。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，11 个 API 测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：点击未开赛列表第一场后，1280px 和 390px 下单场详情正常渲染且无横向溢出。

当前判断：

- 预测入口链路现在成立：未开赛列表 → 单场详情 → 比分分布 → 整届概率传导。
- 后续可以把单场详情升级成独立路由/页面，再接付费解锁。

### 2026-06-14：正式赛事数据导入保护层

已完成：

- 新增 `backend/data_import.py`。
- 新增 `validate_tournament_import_payload`，校验正式导入数据必须包含 48 支球队、12 个小组、每组 4 队和 72 场小组赛。
- 校验球队 `key` / `code` 不能重复。
- 校验小组赛引用球队必须存在，且不能跨组。
- 校验同一对球队不能重复出现。
- 校验 `finished` 比赛必须有比分。
- 新增 `apply_tournament_data_import`，采用“先校验、再备份、再写入”的顺序。
- 新增 `scripts/import_tournament_data.py`。
- 新增 `npm run import:tournament-data`。
- 新增 `docs/赛事数据导入格式.md`，记录导入 JSON 结构、命令和安全机制。
- `.gitignore` 增加 `backend/data_files/backups/`，避免运行时备份误提交。

验证：

- 新增导入器测试：合法 48 队 / 72 场数据会写入 temp data dir 并生成备份。
- 新增失败保护测试：球队不足 48 支时抛错，且不会修改原文件、不会创建备份目录。
- 新增 CLI 测试：`scripts/import_tournament_data.py` 可读取临时导入 JSON 并写入 temp data dir。
- `python3 -m unittest discover -s tests -p test_data_import.py` 通过，3 个导入测试。

当前判断：

- 真实赛事数据替换入口已经具备最低安全线。
- 下一步拿到 FIFA 官方整理数据后，应先生成导入 JSON，运行 `npm run import:tournament-data`，再执行 `npm run validate:data`、`npm run test:model` 和 `npm run build`。

### 2026-06-14：新闻多源交叉验证

已完成：

- `events_from_raw_news` 增加多源归并逻辑。
- 对 `single_source` 且 C 级来源的 raw-news，按 `team + factor + direction` 生成事件签名。
- 同一签名出现两个及以上不同来源时，相关事件自动升级为 `multi_source`。
- 升级后的 C 级事件会按现有规则进入 `apply`，仍保留 C 级低权重。
- 未达到多源条件的 C 级线索仍保持 `watch`，不直接入模。

验证：

- 新增模型测试：两个不同 C 级天气来源指向巴西同一高温风险时，生成事件均为 `multi_source` 且 `action=apply`。
- `python3 -m unittest discover -s tests -p test_model.py` 通过，15 个模型测试。

当前判断：

- 新闻层已经具备最小自动交叉验证能力。
- 后续外部抓取任务写入多来源 raw-news 后，可以自动把重复验证的低级别线索推入低权重模型流。

### 2026-06-14：公开新闻 Feed 导入任务

已完成：

- 新增 `backend/news_feed.py`。
- 新增 `parse_news_feed`，可解析 RSS item 和 Atom entry。
- 新增 `import_news_feed`，把 Feed 条目写入 raw-news。
- 导入条目默认 `status=single_source`，不会绕过来源评级、交叉验证和人工审核。
- 导入器按 `url` 跳过重复新闻，适合定时任务重复执行。
- 新增 `scripts/import_news_feed.py`。
- 新增 `npm run import:news-feed`。
- 新增 `docs/新闻Feed导入说明.md`。

验证：

- 新增 Feed 导入测试：RSS 两条新闻中，已存在 URL 被跳过，新 URL 被追加到 raw-news。
- 新增 CLI 测试：`scripts/import_news_feed.py` 可读取临时 feed.xml 并写入临时 raw-news。
- `python3 -m unittest discover -s tests -p test_news_feed.py` 通过，2 个 Feed 测试。

当前判断：

- 外部新闻抓取现在有可落地入口：抓取器只要输出 RSS/Atom 或本地 feed 文件，就能进入 raw-news → 多源验证 → 审核 → 快照重建链路。
- 后续需要再接真实来源列表和定时任务。

## 十、当前交接摘要

一句话定义：

> 这是一个以未开赛比赛预测为入口、以整届世界杯概率变化为结果的动态预测系统。用户既能看单场谁更可能赢，也能看这场比赛会怎样改变各队晋级、路径和夺冠概率。

当前最重要的开发优先级：

1. 从 FIFA 官方来源整理真实 48 队名单、分组和 72 场小组赛，生成导入 JSON 并走导入器替换当前样例数据。
2. 接真实新闻来源列表和定时任务，把多来源抓取结果写入 `raw-news.json`。
3. 规划独立后台页，承载 raw-news 录入、审核、赛果和伤停信息，不继续挤压预测首页。
4. 把单场详情升级为独立路由/页面，并接付费解锁边界。
5. 最后接支付、用户权限和付费解锁入口。

当前最重要的风险：

- 数据授权风险。
- 新闻传闻误导模型。
- 客户把产品理解成投注建议。
- 底层模型过复杂导致第一版无法快速交付。
- 页面只炫不清楚，用户看不懂概率为什么变化。
- 把“比分概率”误写成“赔率”，导致产品被误解为投注工具。

当前建议：

- 第一版先做“看起来专业、逻辑站得住、可每日更新”的 MVP。
- 不追求一次性把所有因子完全量化。
- 不优先做进行中实时预测，除非有可靠实时数据源。
- 优先做未开赛单场预测和整届概率传导。
- 先把伤病、教练、核心球员、更衣室、天气旅程作为概率变化原因和人工修正项。
- 等真实数据和客户预算明确后，再逐步增强自动化与模型精度。
