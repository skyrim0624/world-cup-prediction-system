# 模拟引擎开发记录

日期：2026-06-14

## 本次目标

把前端 mock 原型推进到“真模型 MVP”：至少能用本地数据跑出单场胜平负、比分概率、路径传导和冠军概率。

## 已完成内容

- 新增 `backend/data.py`：球队评分、赛程、已结束赛果、结构化事件和来源权重。
- 新增 `backend/model.py`：Poisson 单场比分概率、事件修正、已结束比赛锁定、蒙特卡洛路径模拟。
- 新增 `backend/main.py`：FastAPI 接口 `/api/match-prediction`。
- 新增 `requirements.txt`：FastAPI 和 Uvicorn 依赖。
- 新增 `tests/test_model.py`：模型基本测试。
- 更新 `vite.config.ts`：开发环境下 `/api` 代理到 `127.0.0.1:8000`。
- 更新前端：读取接口返回的 `teams` 和 `modelMeta`，显示真实模型、模拟次数和锁定赛果。
- 后续已补齐 48 队赛制骨架、官方淘汰赛路径、Annexe C 小组第三分支、新闻事件审核、Feed 导入、日更流水线、日更状态、进行中比分和预测快照。

## 关键决策

- 第一版不接外部商业数据，先用自建评分和本地事件样例。
- 第一版模拟次数设为 8,000 次，保证开发联调速度；正式更新再提高。
- D 级社媒传闻权重为 0，只展示为风险线索，不进入模型。
- 已结束比赛直接锁定进积分表，不再预测。

## 未解决问题

- 48 队结构、路径和第三名分支已经补齐，但真实官方名单 / 分组 / 赛程尚未替换当前样例数据。
- 新闻已有 raw-news、审核、多源交叉验证、Feed 导入、日更流水线和日更状态落盘，但还没有真实来源列表、外部定时任务和失败告警。
- 已有 `/admin` 独立运营后台雏形，可承载新闻录入、事件审核、快照重建和比分写入；已有轻量 token 写入权限和审计记录；但还没有正式用户账号 / 角色体系。
- 还没有支付和权限。

## 下一步

- 导入真实官方 48 队名单、分组和 72 场小组赛。
- 接真实新闻来源列表，把外部抓取任务接入日更命令。
- 扩展 `/admin` 后台，补真实外部调度、失败告警和用户账号 / 角色体系。
- 接支付和权限。

## 迭代记录：数据 JSON 化与 8 队样例

日期：2026-06-14

### 本次目标

把模型从“代码里写死的 4 队样例”推进到更可维护的本地数据结构，并修正多组情况下的小组排名逻辑。

### 已完成内容

- 新增 `backend/data_files/teams.json`。
- 新增 `backend/data_files/fixtures.json`。
- 新增 `backend/data_files/events.json`。
- 新增 `backend/data_files/source-weights.json`。
- 新增 `backend/data_files/current-match.json`。
- `backend/data.py` 改为从 JSON 加载数据，并做基础校验。
- 球队池扩展为 8 队，两组样例：E 组和 F 组。
- 修正 `rank_group`：只对指定小组内的球队排序。
- `simulate_tournament` 改为按组出线后再进行跨组淘汰路径模拟。
- `/api/model-status` 暴露数据覆盖范围和已知缺口。
- 前端展示事件透明度：入模事件和忽略事件数量。

### 验证结果

- `npm run test:model` 通过。
- `npm run build` 通过。
- 桌面 1280px 和手机 390px 联调无横向溢出。
- 页面显示真实模型、8 队数据和事件透明度。

### 当前判断

这一版已经比“演示模型”更接近真实产品底座：数据可维护、接口透明、分组逻辑正确。但还不是完整世界杯模型，下一步必须继续扩真实赛制和数据运营能力。

## 迭代记录：模拟次数配置化

日期：2026-06-14

### 本次目标

把模拟次数从代码固定值改成接口可配置，支持开发快跑和正式更新两种使用方式。

### 已完成内容

- `build_match_prediction` 支持传入 `simulation_count`。
- `/api/match-prediction` 支持 `simulations` 查询参数。
- FastAPI 对参数做范围限制：最小 1,000，最大 50,000。
- `modelMeta.simulationCount` 返回实际使用的模拟次数。
- 增加 API 测试，验证 `?simulations=1200` 生效。

### 验证结果

- `npm run test:model` 通过。
- `npm run build` 通过。
- 本地 50,000 次模拟成功返回。

### 当前判断

开发预览继续用 8,000 次；正式每日更新可以使用 50,000 次。后续如果数据扩大到 48 队，需要继续做缓存或后台任务，避免前台请求等待太久。

## 迭代记录：每日预测快照

日期：2026-06-14

### 本次目标

让模型具备每日预计算能力，避免正式产品每次页面访问都现场跑完整模拟。

### 已完成内容

- 新增 `backend/snapshot.py`。
- 新增 `write_prediction_snapshot`，可生成预测 JSON 快照。
- 新增 `scripts/update_prediction_snapshot.py` 命令入口。
- 新增 `npm run update:snapshot`。
- 快照默认写入 `backend/snapshots/latest-match-prediction.json`。
- 快照文件不进 Git，只提交生成逻辑。

### 验证结果

- `npm run test:model` 通过。
- `npm run build` 通过。
- 使用 `--simulations 1200 --output /tmp/world-cup-latest.json` 生成快照成功。

### 当前判断

现在模型不仅能响应 API，也能作为每日任务预先生成结果。后续接入完整赛事数据后，应让正式站优先读取快照，后台任务负责定时刷新。

## 迭代记录：48 队赛制骨架与 32 强路径

日期：2026-06-14

### 本次目标

把模型从 8 队样例推进到 2026 世界杯核心赛制骨架：48 队、12 组、最佳第三出线和 32 强淘汰路径。

### 已完成内容

- `backend/data.py` 自动补齐 48 个参赛槽位。
- 每组自动补足到 4 队。
- 自动生成每组 6 场小组赛，共 72 场小组赛结构。
- 新增 `group_names`。
- 新增 `best_third_place_teams`。
- `simulate_tournament` 输出 `roundOf32`、`roundOf16`、`quarterfinal`、`semifinal`、`final`、`champion`。
- 比分采样改为累计分布二分采样，减少模拟耗时。
- 测试从默认 8,000 次改为 1,200 次，保证反馈速度。

### 验证结果

- `npm run test:model` 通过，13 个测试。
- `npm run build` 通过。
- 1,200 次完整预测约 2.5 秒。

### 当前判断

赛制骨架已经接近客户要的“动态预测模型”底层，但仍不能声称完全真实。下一步要继续补官方 32 强路径映射、小组第三分支表，以及真实名单/赛程导入机制。

## 迭代记录：事件因子与五大盘面接入

日期：2026-06-14

### 本次目标

把新闻/事件修正从“只改变底层评分”推进到产品可解释层：用户能看到可信事件具体影响哪个盘面。

### 已完成内容

- 新增 `event_factor_impacts`，按来源权重输出每队 attack / defense / goalkeeper / path / squad 的事件影响。
- `apply_event_adjustments` 改为先汇总事件影响，再统一修正球队底层评分。
- 后端返回 `modelMeta.factorImpacts`，前端可展示事件影响明细。
- 后端球队输出从底层指标改为五大盘面：实力盘、状态盘、路径盘、人员盘、边际盘。
- 前端取消 8 队 `TeamKey` 硬编码，支持 48 队和可替换槽位。
- 占位球队使用队码 fallback，不再出现空旗帜。
- 五大盘面 UI 显示当前事件修正值，例如状态盘 `-0.7`。
- 前端交互请求显式使用 1,200 次模拟，刷新间隔从 5 秒调整为 15 秒，避免请求堆积。
- `npm run dev:api` 去掉 `uvicorn --reload`，解决计算接口在 reload 模式下被拖慢的问题。

### 验证结果

- `npm run test:model` 通过，15 个测试。
- `npm run build` 通过。
- 稳定 API 模式下，`/api/match-prediction?simulations=1200` 经 Vite 代理约 0.75 秒返回。
- 浏览器自动化检查：桌面 1280px 无溢出；手机 390px 页面宽度等于视口；页面进入 `真实模型`；48 队冠军榜渲染完整。

### 当前判断

这一版已经具备客户要求的动态预测核心闭环：未开赛单场预测、比分概率、已完赛锁定、路径传导、48 队赛制骨架、事件权重、五大盘面解释和前端真实模型展示。仍不能称为最终成品，因为官方赛程路径、真实名单、新闻抓取和后台运营还没有完成。

## 迭代记录：官方淘汰赛路径与接口短缓存

日期：2026-06-14

### 本次目标

把 32 强后的路径从 MVP 排序配对改成 FIFA Regulations 里的固定 match number 路径，并解决前端多标签/轮询重复触发模拟导致的响应变慢问题。

### 已完成内容

- 根据 FIFA World Cup 26 Regulations 2026 年 5 月版 Article 12.6-12.11，加入 M73-M88、M89-M96、M97-M100、M101-M102 的固定路径。
- `simulate_tournament` 不再把 32 强球队按排名排序后两两相邻配对，而是按官方 match number 逐轮推进。
- 新增 `build_round_of_32_matches`，用小组第一/第二和最佳第三名生成 32 强对阵。
- 新增第三名槽位约束匹配：按官方每个第三名槽位允许的小组集合分配最佳第三名。
- 修复最佳第三名重复排名 bug：最佳第三名必须复用同一次小组排名，避免同一支队同时成为小组第二和第三。
- 新增 15 秒 API 短缓存，同一 `simulations` 参数在一个刷新窗口内复用结果，避免前端轮询和多标签重复重算。
- 前端开发环境直接请求 `127.0.0.1:8000`，生产环境仍使用相对 `/api`。

### 验证结果

- `npm run test:model` 通过，17 个测试。
- `npm run build` 通过。
- 直连 API：首次 `/api/match-prediction?simulations=1200` 约 0.9 秒，缓存命中约 0.013 秒。
- 浏览器自动化检查：桌面和手机都进入 `真实模型`，48 队冠军榜完整，五大盘面完整，页面无横向滚动。

### 当前判断

官方固定路径已经接入，模型路径可信度明显提升。仍未完成的是 Annexe C 的 495 种第三名组合完整表；当前使用的是官方槽位允许集合的确定性匹配，不会产生无效 32 强，但还不是完整 Annex C 数据表。

## 迭代记录：Annex C 第三名组合表数据化

日期：2026-06-14

### 本次目标

把 FIFA Regulations Annexe C 的 495 种第三名组合完整接入模型，替换上一轮的确定性约束匹配。

### 已完成内容

- 从 FIFA World Cup 26 Regulations 2026 年 5 月版 PDF 中抽取 Annexe C 表格。
- 新增 `backend/data_files/third-place-combinations.json`，包含 495 种组合。
- 数据结构使用晋级第三名小组集合做 key，使用 1A / 1B / 1D / 1E / 1G / 1I / 1K / 1L 对应的第三名小组做 value。
- `backend/data.py` 加载并校验组合数必须为 495。
- `assign_third_place_slots` 优先查 Annex C 表；只有数据缺失时才退回约束匹配。
- 新增测试，验证 495 表加载和已知组合 `EFGHIJKL` 的映射。

### 验证结果

- `npm run test:model` 通过，18 个测试。
- `npm run build` 通过。

### 当前判断

路径模型里的最大结构性缺口已经补上：32 强固定路径、16 强后续路径和 495 种第三名组合都已按 FIFA Regulations 接入。下一步模型层应转向真实名单/赛程导入和新闻事件自动化。

## 迭代记录：正式接口快照优先

日期：2026-06-14

### 本次目标

让正式访问默认读取每日预计算快照，避免每个用户请求都现场跑完整蒙特卡洛。

### 已完成内容

- 新增 `read_prediction_snapshot`。
- `/api/match-prediction` 默认优先读取 `backend/snapshots/latest-match-prediction.json`。
- 如果快照不存在，接口自动回退到现场计算。
- 如需强制现场计算，可传 `useSnapshot=false`。
- 补充快照读取测试：文件存在时返回 payload，文件不存在时返回 `None`。

### 验证结果

- `npm run test:model` 通过，20 个测试。
- `npm run build` 通过。

### 当前判断

正式站现在具备“后台定时生成快照、前台优先读快照、缺快照再现场计算”的基础架构。后续只需要把 `npm run update:snapshot` 放入定时任务或后台运营按钮。

## 迭代记录：预测数据校验命令

日期：2026-06-14

### 本次目标

在替换真实名单、分组、赛程和新闻事件前，先提供独立数据校验入口，避免 JSON 数据错误直接污染模型输出。

### 已完成内容

- 新增 `scripts/validate_prediction_data.py`。
- 新增 `npm run validate:data`。
- 校验内容包括：48 队、12 组、每组 4 队、至少 72 场小组赛、495 个 Annex C 组合、当前比赛引用有效、事件引用球队有效。
- 新增数据校验测试。

### 验证结果

- `npm run validate:data` 通过。
- `npm run test:model` 通过，21 个测试。
- `npm run build` 通过。

### 当前判断

现在真实数据导入有了最低安全线。后续替换真实 48 队名单和赛程时，必须先跑 `npm run validate:data`，再跑模型测试和构建。

## 迭代记录：新闻原始数据到事件审核管线

日期：2026-06-14

### 本次目标

把新闻事件从直接写结构化 `events.json`，升级为“原始新闻输入 → 来源评级 → 事件候选 → 入模/待审核/忽略”的数据流。

### 已完成内容

- 新增 `backend/data_files/news-sources.json`，维护新闻来源和 S/A/B/C/D 等级。
- 新增 `backend/data_files/raw-news.json`，作为后续抓取器或人工录入的原始新闻输入。
- 新增 `NewsSource`、`RawNewsItem` 数据结构。
- 增加原始新闻校验：来源必须存在，引用球队必须存在，新闻 id 不能重复。
- 新增关键词规则，把原始新闻推断为事件因子、方向和强度。
- 新增事件动作：`apply` 入模、`watch` 待审核、`ignore` 忽略。
- 模型只让 `action=apply`、来源权重大于 0、且绑定具体球队的事件进入概率修正。
- `/api/events` 暴露事件列表、来源等级、审核动作和摘要。
- `event_summary` 增加 `reviewRequired`。
- 没有绑定具体球队的事件展示为“全局备注”，不再误写成“可入模型”。
- `npm run validate:data` 增加新闻来源和原始新闻统计校验。

### 验证结果

- `npm run validate:data` 通过。
- `npm run test:model` 通过，22 个测试。
- `npm run build` 通过。
- `/api/events` 返回摘要：7 条事件，3 条入模，2 条忽略，1 条待审核，4 条原始新闻。

### 当前判断

新闻层已经从静态样例推进到可运营的数据流雏形。后续外部抓取任务只需要写入 `raw-news.json` 或同结构数据源，再由后台审核决定是否入模。

## 迭代记录：新闻审核写入命令

日期：2026-06-14

### 本次目标

在后台页面之前，先补可复用的审核写入能力，让运营可以把 raw news 标记为确认、多源、单源、未证实或传闻。

### 已完成内容

- 新增 `backend/event_review.py`。
- 新增 `review_raw_news_item`，可更新指定 raw news 的 `status` 和 `team`。
- 新增 `scripts/review_news_item.py`。
- 新增 `npm run review:news`。
- 新增审核写入测试。

### 验证结果

- 使用临时 raw-news 文件执行 `npm run review:news -- --id mexico-heat-watch --status confirmed --team brazil` 成功。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，23 个测试。
- `npm run build` 通过。

### 当前判断

后台审核页面还未实现，但审核动作已有可复用后端函数和 CLI。后续后台 UI 应调用同一套逻辑，而不是另写一套审核规则。

## 迭代记录：新闻审核 API 与前端运营面板

日期：2026-06-14

### 本次目标

把 CLI 审核能力接入 Web/API，让新闻事件从“待审核”可以在网页里完成确认或忽略，并让多源确认后的 C 级事件进入低权重模型流。

### 已完成内容

- `TeamEvent` 增加 raw news `id`，`/api/events` 返回事件唯一标识，前端可定位待审核项。
- 新增 `POST /api/events/review`，复用 `review_raw_news_item` 写入 raw-news 审核状态和绑定球队。
- CORS 增加 POST 支持。
- `model-status` 明确标注：审核写入后需要重建快照或重启进程，当前运行模型暂不热重载。
- C 级来源在 `confirmed` 或 `multi_source` 后可从 `watch` 升级为 `apply`，仍按 C 级低权重入模。
- 前端新增“事件审核”模块，展示原始新闻、入模、待审、忽略数量。
- 前端待审核项支持“多源确认”和“忽略”两个动作，写入成功后本地更新队列状态，并提示需要重建快照。

### 验证结果

- 新增 API 测试：事件列表暴露 raw news id，审核接口可写入临时 raw-news 文件。
- 新增模型测试：多源确认后的 C 级来源可以进入审核后的模型流。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，26 个测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：1280px 和 390px 下审核面板无横向溢出，待审核按钮渲染正常。

### 当前判断

新闻事件从“原始输入 → 来源评级 → 待审 → 人工确认/忽略”的运营闭环已经成立。现在最大缺口变成审核后的热重载与快照触发：审核结果已经写入 raw-news，但正式预测仍需要重建快照或重启 API 后完全反映。

## 迭代记录：审核后快照重建闭环

日期：2026-06-14

### 本次目标

补上审核写入后的运营闭环：事件确认后不再依赖重启 API，而是可以在同一进程重新加载 raw-news 并生成新的预测快照。

### 已完成内容

- 数据层新增 `RuntimeDataset`，可按指定 raw-news 文件重新构建球队、赛程、来源、原始新闻、事件和元信息。
- 新增 `reload_runtime_data`，用于在运行中刷新数据层全局状态。
- 模型层新增 `reload_model_data`，同步刷新模型使用的球队、赛程、事件、来源权重、当前比赛和 Annex C 数据。
- 新增 `POST /api/snapshot/rebuild`，先按当前 raw-news 路径 reload 模型，再写入预测快照。
- `/api/match-prediction` 改为读取可配置的 `snapshot_data_path`，测试和运营可以复用同一入口。
- 前端“事件审核”模块新增“重建快照”按钮，成功后直接用新快照刷新预测页面。
- 删除 `model-status` 中“运行中模型暂不热重载”的过时限制。

### 验证结果

- 新增 API 测试：审核临时 raw-news 后立即重建快照，断言新新闻以“轻微修正”进入快照输出。
- `npm run test:model` 通过，27 个测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：1280px 和 390px 下“重建快照”按钮无横向溢出。

### 当前判断

新闻事件运营链路现在闭合为：raw-news 输入 → 网页审核 → 多源确认/忽略 → 重建快照 → 前台读取新快照。下一步应补外部新闻抓取和后台录入，而不是继续扩前端展示。

## 迭代记录：原始新闻录入 API

日期：2026-06-14

### 本次目标

给外部新闻抓取器和后续后台表单提供统一写入口，让新线索进入 `raw-news` 后马上出现在事件审核队列里。

### 已完成内容

- 新增 `backend/news_ingest.py`。
- 新增 `append_raw_news_item`，支持向 raw-news JSON 追加新闻线索，并校验状态和重复 id。
- 新增 `POST /api/raw-news`。
- 接口会校验新闻来源是否存在、球队 key 是否有效。
- 录入后立即 `reload_model_data`，让 `/api/events` 不重启即可看到新增线索。
- 录入只进入审核队列，不绕过来源评级、人工审核和快照重建流程。

### 验证结果

- 新增 API 测试：向临时 raw-news 追加新闻后，文件写入成功，`/api/events` 立即返回新增事件。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，8 个 API 测试。

### 当前判断

后台录入的底层接口已经有了。前端暂时不扩复杂表单，避免首页继续膨胀；后续可以在单独后台页或外部抓取任务里调用同一个接口。

## 迭代记录：赛果锁定 API 与审核队列即时刷新

日期：2026-06-14

### 本次目标

把“已结束比赛锁定真实赛果”从静态 JSON 能力升级为可运营 API，并让审核结果写入后立即反映到事件队列。

### 已完成内容

- 数据层 `load_fixtures` 支持按指定 fixtures 文件重载。
- `load_runtime_dataset` 和 `reload_runtime_data` 增加 fixtures 路径参数。
- `reload_model_data` 同步刷新赛程数据，模型可以在同一进程内吃到新赛果。
- 新增 `backend/fixture_update.py`。
- 新增 `record_fixture_result`，可把指定比赛写成 `finished` 并记录 `home_score` / `away_score`。
- 新增 `POST /api/fixtures/result`。
- 赛果写入后立即 reload 模型和清理预测缓存。
- `POST /api/events/review` 写入审核结果后也会立即 reload，让 `/api/events` 不再依赖前端本地假更新。

### 验证结果

- 新增 API 测试：用临时 fixtures 文件写入巴西 2-1 阿根廷后，文件状态变为 finished，`/api/model-status` 锁定赛果数变为 1。
- 增强审核 API 测试：审核临时 raw-news 后，`/api/events` 立即返回更新后的 status 和 team。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，9 个 API 测试。

### 当前判断

动态预测模型的两条基础运营输入已经闭合：新闻事件可以录入/审核，已结束比赛可以锁定真实赛果。下一步应该把这些能力组织到独立后台页，并接外部赛程/新闻抓取任务。

## 迭代记录：未开赛比赛预测列表

日期：2026-06-14

### 本次目标

从“只展示当前单场预测”推进到“用户能扫到多场未开赛比赛的预测走向”，贴合客户要求的每轮未发生比赛胜率走向。

### 已完成内容

- 新增 `build_upcoming_match_predictions`。
- 新增 `GET /api/upcoming-matches`，可按 `limit` 返回未开赛比赛预测列表。
- 每场输出阶段、开赛时间、双方 key/name/code、胜平负概率和最可能比分。
- 胜平负百分比在输出层归一为 100，避免前端展示因四舍五入出现 99/101。
- 前端新增“未开赛预测”模块，默认展示 6 场未开赛比赛。
- 移动端下改为单列卡片，避免概率按钮和球队名挤压。

### 验证结果

- 新增 API 测试：`/api/upcoming-matches?limit=5` 返回 5 场 scheduled 比赛，且胜平负概率合计 100。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，10 个 API 测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：1280px 和 390px 下“未开赛预测”模块无横向溢出，6 场比赛渲染正常。

### 当前判断

首页现在同时具备“当前重点单场预测”和“多场未开赛预测列表”。下一步更适合做单场详情页，把每场的比分分布、路径传导和 AI 分析拆出去，而不是继续向首页堆模块。

## 迭代记录：任意未开赛单场详情

日期：2026-06-14

### 本次目标

让未开赛列表里的任意比赛都能进入单场详情，而不是只有首页当前比赛能看到比分分布和路径传导。

### 已完成内容

- `simulate_tournament` 支持指定 `forced_match`，不再只能强制当前比赛场景。
- `build_scenario_impacts` 对任意传入的 home/away 比赛计算三种赛果的整届概率传导。
- 新增 `score_outcomes_for_match`，复用比分 Top 3 输出逻辑。
- 新增 `build_match_detail`。
- 新增 `GET /api/match-detail`，支持 `home`、`away` 和 `simulations` 参数。
- 前端“未开赛预测”列表可点击选择比赛。
- 前端新增“单场详情”模块，展示胜平负、比分 Top 3 和整届概率传导。

### 验证结果

- 新增 API 测试：`/api/match-detail?home=spain&away=argentina&simulations=1200` 返回对应比赛详情，比分和传导各 3 项，胜平负合计 100。
- `python3 -m unittest discover -s tests -p test_api.py` 通过，11 个 API 测试。
- `npm run build` 通过。
- 浏览器自动化检查通过：点击未开赛列表第一场后，1280px 和 390px 下单场详情正常渲染且无横向溢出。

### 当前判断

预测入口链路现在成立：未开赛列表 → 单场详情 → 比分分布 → 整届概率传导。后续可以把单场详情升级成独立路由/页面，再接付费解锁。

## 迭代记录：正式赛事数据导入保护层

日期：2026-06-14

### 本次目标

为后续替换真实 48 队名单和小组赛赛程提供安全入口，避免手改 JSON 导致模型数据污染。

### 已完成内容

- 新增 `backend/data_import.py`。
- 新增 `validate_tournament_import_payload`，校验正式导入数据必须包含 48 支球队、12 个小组、每组 4 队和 72 场小组赛。
- 校验球队 `key` / `code` 不能重复。
- 校验小组赛引用球队必须存在，且不能跨组。
- 校验同一对球队不能重复出现。
- 校验 `finished` 比赛必须有比分。
- 新增 `apply_tournament_data_import`，采用“先校验、再备份、再写入”的顺序。
- 新增 `scripts/import_tournament_data.py`。
- 新增 `npm run import:tournament-data`。
- 新增 [赛事数据导入格式](赛事数据导入格式.md)，记录导入 JSON 结构、命令和安全机制。
- `.gitignore` 增加 `backend/data_files/backups/`，避免运行时备份误提交。

### 验证结果

- 新增导入器测试：合法 48 队 / 72 场数据会写入 temp data dir 并生成备份。
- 新增失败保护测试：球队不足 48 支时抛错，且不会修改原文件、不会创建备份目录。
- 新增 CLI 测试：`scripts/import_tournament_data.py` 可读取临时导入 JSON 并写入 temp data dir。
- `python3 -m unittest discover -s tests -p test_data_import.py` 通过，3 个导入测试。

### 当前判断

真实赛事数据替换入口已经具备最低安全线。下一步拿到 FIFA 官方整理数据后，应先生成导入 JSON，运行 `npm run import:tournament-data`，再执行 `npm run validate:data`、`npm run test:model` 和 `npm run build`。

## 迭代记录：新闻多源交叉验证

日期：2026-06-14

### 本次目标

减少完全依赖人工审核的新闻流，让不同 C 级来源对同一球队、同一因子、同一方向的线索可以自动升级为多源确认。

### 已完成内容

- `events_from_raw_news` 增加多源归并逻辑。
- 对 `single_source` 且 C 级来源的 raw-news，按 `team + factor + direction` 生成事件签名。
- 同一签名出现两个及以上不同来源时，相关事件自动升级为 `multi_source`。
- 升级后的 C 级事件会按现有规则进入 `apply`，仍保留 C 级低权重。
- 未达到多源条件的 C 级线索仍保持 `watch`，不直接入模。

### 验证结果

- 新增模型测试：两个不同 C 级天气来源指向巴西同一高温风险时，生成事件均为 `multi_source` 且 `action=apply`。
- `python3 -m unittest discover -s tests -p test_model.py` 通过，15 个模型测试。

### 当前判断

新闻层已经具备最小自动交叉验证能力。后续外部抓取任务写入多来源 raw-news 后，可以自动把重复验证的低级别线索推入低权重模型流。

## 迭代记录：公开新闻 Feed 导入任务

日期：2026-06-14

### 本次目标

为外部新闻抓取建立最低可运行入口：先支持公开 RSS / Atom Feed 导入，不绑定未授权商业 API。

### 已完成内容

- 新增 `backend/news_feed.py`。
- 新增 `parse_news_feed`，可解析 RSS item 和 Atom entry。
- 新增 `import_news_feed`，把 Feed 条目写入 raw-news。
- 导入条目默认 `status=single_source`，不会绕过来源评级、交叉验证和人工审核。
- 导入器按 `url` 跳过重复新闻，适合定时任务重复执行。
- 新增 `scripts/import_news_feed.py`。
- 新增 `npm run import:news-feed`。
- 新增 [新闻 Feed 导入说明](新闻Feed导入说明.md)。

### 验证结果

- 新增 Feed 导入测试：RSS 两条新闻中，已存在 URL 被跳过，新 URL 被追加到 raw-news。
- 新增 CLI 测试：`scripts/import_news_feed.py` 可读取临时 feed.xml 并写入临时 raw-news。
- `python3 -m unittest discover -s tests -p test_news_feed.py` 通过，2 个 Feed 测试。

### 当前判断

外部新闻抓取现在有可落地入口：抓取器只要输出 RSS/Atom 或本地 feed 文件，就能进入 raw-news → 多源验证 → 审核 → 快照重建链路。后续需要再接真实来源列表和定时任务。

## 迭代记录：每日更新流水线

日期：2026-06-14

### 本次目标

把分散的新闻导入、模型重载和快照重建组织成一条可重复执行的日更流程，方便后续接定时任务或后台按钮。

### 已完成内容

- 新增 `backend/daily_update.py`。
- 新增 `FeedSpec` 和 `load_feed_specs`，支持从 JSON 配置批量读取 Feed 输入。
- 新增 `run_daily_update`，按顺序执行 Feed 导入、模型重载和预测快照生成。
- 日更报告输出新闻新增数、跳过数、快照路径、模拟次数、锁定赛果和事件摘要。
- 新增 `scripts/run_daily_update.py`。
- 新增 `npm run daily:update`。
- 新增 [日更流程说明](日更流程说明.md)。

### 验证结果

- 新增日更函数测试：临时 Feed 导入 1 条新闻后生成预测快照，快照模拟次数为 1200。
- 新增日更 CLI 测试：`scripts/run_daily_update.py` 可读取临时 feed-config 并输出“日更流程完成”。
- `npm run test:model` 通过，43 个测试。

### 当前判断

系统现在具备运营日更骨架：外部抓取器只要生成 Feed 文件，日更命令就能导入新闻、刷新模型并生成最新快照。下一步要接真实来源列表、外部调度和失败告警。

## 迭代记录：进行中比分动态权重

日期：2026-06-14

### 本次目标

补齐“已开始比赛作为动态权重因子”的后端逻辑，不再只有未开赛预测和已完赛锁定两种状态。

### 已完成内容

- 新增 `record_fixture_live_score`，可把比赛状态写为 `live` 并记录当前比分。
- 新增 `POST /api/fixtures/live`。
- `/api/model-status` 新增 `liveMatches`。
- 新增 `LIVE_REMAINING_GOAL_RATE`，在缺少比赛分钟字段时用保守剩余进球比例处理进行中比赛。
- 新增 `sample_fixture_score`，`live` 比赛会以当前比分为起点继续采样后续进球。
- `simulate_tournament` 已改为通过 `sample_fixture_score` 处理非完赛比赛。
- 赛事导入和运行时加载均要求 `live` 比赛必须有当前比分。

### 验证结果

- 新增模型测试：`live` 比赛当前 2-0 时，模拟采样比分不会低于当前主队进球。
- 新增 API 测试：`POST /api/fixtures/live` 可把临时赛程写成 live，并让 `/api/model-status` 返回 `liveMatches=1`。
- 新增导入测试：`live` 比赛没有当前比分时，导入器拒绝写入。
- `npm run test:model` 通过，43 个测试。

### 当前判断

动态模型现在覆盖三类比赛状态：未开赛正常预测、进行中比分进入模拟、已完赛比分锁定。第一版仍不承诺强实时，后续需要外部比分源或后台录入持续写入 live score。

## 迭代记录：独立运营后台雏形

日期：2026-06-14

### 本次目标

把预测页和运营动作拆开，先提供一个独立后台入口承载日更模型需要的人工作业。

### 已完成内容

- 新增 `backend/admin.py`。
- 新增 `GET /api/admin/overview`，返回赛程状态、事件摘要、待审队列、快照状态和运营端点。
- 前端新增 `/admin` 路径。
- 后台页展示赛程状态、事件状态、日更命令、快照重建、比分写入和事件审核。
- 比分写入支持 `live` 和 `finished` 两种模式。
- 后台页新增新闻录入表单，写入 `/api/raw-news` 后刷新事件待审队列。
- 新增 `WORLD_CUP_ADMIN_TOKEN` 轻量后台写入权限；设置后，写接口需要 `X-Admin-Token`。
- 新增 `backend/admin_audit.py`，后台写操作会写入 `backend/data_files/admin-audit.jsonl`。
- `/admin` 支持保存后台 token，并展示最近审计记录。
- `/api/model-status` 的 known gaps 更新为：后台权限仍为轻量 token，暂未接用户账号、角色和支付权限。
- 新增 `POST /api/admin/tournament-data/import`，后台可导入 48 队和 72 场小组赛 JSON。
- 数据层 `reload_model_data` 支持按指定 teams/fixtures 文件重载，导入后可立即刷新模型。
- `/admin` 新增数据健康模块，显示球队数、赛程数和占位槽位数量。
- `/admin` 新增赛事导入表单，可粘贴官方整理 JSON 并触发导入。

### 验证结果

- 新增 API 测试：`/api/admin/overview` 可返回临时赛程里的未开赛、进行中和已完赛数量，并返回待审队列和 `npm run daily:update`。
- 新增 API 测试：设置 `WORLD_CUP_ADMIN_TOKEN` 后，未带 token 的写接口返回 401；带 token 的新闻录入会生成审计记录。
- 新增 API 测试：后台赛事导入可写入临时 teams/fixtures，生成备份，刷新模型并写审计记录。
- `npm run test:model` 通过，47 个测试。
- `npm run build` 通过。
- 浏览器检查通过：`/admin` 在 1280px 和 390px 下无横向溢出，能看到后台标题、新闻录入、比分写入、事件审核和日更命令。
- 浏览器检查通过：启用 token 后，`/admin` 能看到 token 提示和审计记录区域。
- 浏览器检查通过：`/admin` 能看到数据健康和赛事导入表单。

### 当前判断

后台已经有最小运营入口，不必继续把审核和比分写入堆在预测首页。下一步应补真实外部调度、失败告警和正式用户账号 / 角色体系。

## 迭代记录：日更状态落盘与后台可视化

日期：2026-06-14

### 本次目标

让日更流程不仅能执行，还能被后台判断最近一次是否成功、导入了多少新闻、生成了多少次模拟的预测快照。

### 已完成内容

- `run_daily_update` 新增 `status_path`，日更成功后写入最近一次运行状态。
- `scripts/run_daily_update.py` 新增 `--status` 参数，默认写入 `backend/data_files/daily-update-status.json`。
- 状态内容包含执行状态、更新时间、Feed 新增 / 跳过数量、快照路径、模拟次数、锁定赛果、进行中比赛和事件摘要。
- 日更失败时会写入 `status=failed`、失败时间和错误原因，后台可区分未执行与执行失败。
- 新增 `read_daily_update_status`。
- `GET /api/admin/overview` 新增 `dailyUpdateStatus`。
- `/admin` 的日更卡片展示最近日更时间、执行状态、新增新闻和模拟次数，并兼容失败状态的稀疏结构。
- `.gitignore` 忽略运行时日更状态文件。

### 验证结果

- 新增日更函数测试：执行日更后写入 status JSON。
- 新增日更 CLI 测试：`scripts/run_daily_update.py --status ...` 可生成状态文件。
- 新增日更失败测试：Feed 配置缺失时 CLI 非零退出，并写入失败状态和错误原因。
- 新增 API 测试：`/api/admin/overview` 可返回 `dailyUpdateStatus`。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，51 个测试。
- `npm run build` 通过。
- 浏览器检查通过：`/admin` 在 1280px 和 390px 下显示日更状态，无横向溢出；token 输入交互生效。
- 浏览器检查通过：日更失败状态下 `/admin` 在 1280px 和 390px 下显示“失败”，无错误遮罩和横向溢出。

### 当前判断

日更链路现在不只是“能跑”，运营后台也能判断今天是否跑过、跑出了多少新闻、多少次模拟，以及失败原因。下一步要补真实外部调度和更主动的失败告警。

## 迭代记录：赛事数据回滚入口

日期：2026-06-14

### 本次目标

补齐正式赛事数据导入后的恢复能力，避免官方名单或赛程导入错误后只能手工改文件。

### 已完成内容

- 新增 `restore_tournament_backup`，可从指定备份目录恢复 `teams.json` 和 `fixtures.json`。
- 回滚前会先备份当前数据，保留二次恢复空间。
- 回滚会拒绝非法备份 ID 和路径穿越。
- 新增 `POST /api/admin/tournament-data/rollback`。
- 回滚成功后会重载模型、清空预测缓存，并写入后台审计日志。
- `/api/admin/overview` 新增 `tournamentRollbackEndpoint`。
- `/admin` 的赛事导入卡片新增备份目录 ID 输入和“回滚赛事数据”按钮。
- [赛事数据导入格式](赛事数据导入格式.md) 新增后台回滚说明。

### 验证结果

- 新增底层回滚测试：恢复指定备份，同时把当前数据写入新的备份目录。
- 新增路径穿越防护测试：非法备份 ID 会被拒绝。
- 新增 API 回滚测试：后台回滚可恢复临时 teams/fixtures、刷新模型并写审计日志。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，54 个测试。
- `npm run build` 通过。
- 浏览器检查通过：`/admin` 在 1280px 和 390px 下显示回滚入口，无错误遮罩和横向溢出。

### 当前判断

赛事数据导入现在具备“校验、备份、写入、可回滚”的最小安全闭环。下一步可以补备份列表展示和更细的后台角色权限。

## 迭代记录：赛事备份列表展示

日期：2026-06-14

### 本次目标

让后台能直接看到可回滚备份，避免运营人员必须从导入结果或审计日志里手动复制备份目录 ID。

### 已完成内容

- 新增 `list_tournament_backups`，按备份目录 ID 倒序列出最近完整备份。
- 不完整备份不会进入可回滚列表。
- `GET /api/admin/overview` 新增 `tournamentBackups`。
- `/admin` 的赛事导入卡片新增最近备份列表。
- 点击“选择”会自动填入回滚备份 ID。

### 验证结果

- 新增底层备份列表测试：只返回完整备份，并按新到旧排序。
- 新增后台概览测试：`/api/admin/overview` 返回 `tournamentBackups`。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，56 个测试。
- `npm run build` 通过。
- 浏览器检查通过：`/admin` 在 1280px 和 390px 下显示备份列表；桌面端点击“选择”后回滚输入框填入备份 ID；无错误遮罩和横向溢出。

### 当前判断

赛事数据恢复链路现在已经可被后台直接操作。下一步更应该补真实外部调度告警、正式角色权限和付费权限。

## 迭代记录：日更新鲜度与健康状态

日期：2026-06-14

### 本次目标

让后台能区分“没有执行日更”“日更成功但过期”“日更失败”，而不是只显示一份最近状态文件。

### 已完成内容

- 新增 `build_daily_update_health`。
- 支持 `未执行`、`正常`、`过期`、`失败` 四类日更健康状态。
- 成功日更超过 24 小时会被标记为 `过期`。
- 失败日更会把错误原因纳入健康说明。
- `GET /api/admin/overview` 新增 `dailyUpdateHealth`。
- `/admin` 日更卡片新增“日更健康”。

### 验证结果

- 新增日更健康测试：覆盖未执行、24 小时内成功、超过 24 小时过期和失败状态。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，60 个测试。
- `npm run build` 通过。
- 浏览器检查通过：`/admin` 在 1280px 和 390px 下显示“日更健康”，无错误遮罩和横向溢出。

### 当前判断

后台现在能判断日更是否仍然新鲜。下一步要把健康状态接入真实外部调度和告警。

## 迭代记录：日更健康检查 CLI

日期：2026-06-14

### 本次目标

把后台里的日更健康判断变成外部调度和监控也能消费的命令行检查。

### 已完成内容

- 新增 `scripts/check_daily_update_health.py`。
- 新增 `npm run daily:check`。
- 健康状态为 `正常` 时退出码为 0。
- 健康状态为 `未执行`、`过期`、`失败` 时退出码为 1。
- [日更流程说明](日更流程说明.md) 新增健康检查命令说明。

### 验证结果

- 新增 CLI 测试：24 小时内成功状态返回 0 并输出“日更健康: 正常”。
- 新增 CLI 测试：过期状态返回非 0 并输出“日更健康: 过期”。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，62 个测试。
- `npm run build` 通过。

### 当前判断

日更健康已经可以被 cron、GitHub Actions 或服务器监控消费。下一步是在真实部署环境配置具体告警通道。

## 迭代记录：赛程编号、城市与场馆元数据

日期：2026-06-14

### 本次目标

让赛事数据结构提前承接真实世界杯赛程里的比赛编号、城市和场馆，为后续天气、旅程、恢复时间和场馆边际因子提供稳定字段。

### 已完成内容

- `Fixture` 数据结构新增 `match_no`、`city`、`stadium` 三个可选字段。
- `GET /api/match-prediction`、`GET /api/upcoming-matches`、`GET /api/match-detail` 会透出赛程编号、城市和场馆。
- 前端首页主对阵、未开赛比赛列表和单场详情页会在有数据时显示 `场馆 · 城市`。
- 赛事导入校验新增元数据类型约束：`match_no` 必须是数字，`city` 和 `stadium` 必须是文本。
- [赛事数据导入格式](赛事数据导入格式.md) 已补充赛程元数据示例和字段规则。

### 验证结果

- 新增 API 测试：`/api/upcoming-matches` 会保留并返回赛程编号、城市和场馆。
- 新增导入测试：赛事导入会拒绝错误类型的赛程元数据，并保留合法元数据。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，65 个测试。
- `npm run build` 通过。
- 浏览器检查通过：首页在 1280px 和 390px 下无错误遮罩和横向溢出，点击未开赛比赛后单场详情可加载。
- 浏览器注入场馆元数据检查通过：`Estadio Azteca · Mexico City` 会在首页主对阵、未开赛列表和单场详情中显示。

### 当前判断

赛程数据已经具备承接天气、旅行、恢复时间和场馆边际因子的基础字段。下一步要从官方来源整理真实赛程城市/场馆数据，并把这些字段纳入实际导入数据。

## 迭代记录：旅行与恢复边际因子接入单场模型

日期：2026-06-14

### 本次目标

把场馆和城市元数据进一步转成模型可用的边际因子，让“旅行、城市转场、恢复时间”不只停留在文档和展示层。

### 已完成内容

- 新增开赛时间解析，支持 ISO 时间和当前样例里的 `6月15日 08:00` 格式。
- 新增 `build_fixture_context`，可根据同队上一场比赛计算休息天数、上一场城市、当前城市、是否转场和边际影响。
- 新增 `apply_fixture_context_adjustments`，对短休和跨城市转场球队的当前单场 `path` 与 `squad` 做小幅下调。
- 单场主预测、未开赛比赛列表和单场详情都会使用当前比赛的旅行/恢复上下文计算胜平负和比分分布。
- 当有负面旅行/恢复上下文时，AI 分析会追加边际盘说明。

### 验证结果

- 新增模型测试：短休 3 天且从 Los Angeles 转场到 Mexico City 会产生负向边际影响。
- 新增模型测试：受影响球队的 `path` 和 `squad` 会按边际影响下调，未受影响球队不变。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，67 个测试。
- `npm run build` 通过。

### 当前判断

旅行、城市转场和恢复时间已经进入当前单场预测模型。下一步仍然是导入真实官方赛程城市和场馆数据，否则这个因子在样例数据里不会大规模生效。

## 迭代记录：赛事导入同步 current-match 防护

日期：2026-06-14

### 本次目标

避免导入真实赛程后，旧样例 `current-match.json` 指向不存在的比赛，导致首页主预测接口崩溃。

### 已完成内容

- 赛事导入 payload 支持可选 `currentMatch`。
- 如果提供 `currentMatch`，导入器会校验该对阵存在于新赛程，并同步写入 `backend/data_files/current-match.json`。
- 如果不提供 `currentMatch`，但当前目录已有 `current-match.json`，导入器会校验旧主预测比赛仍存在于新赛程；不存在则拒绝导入。
- 赛事备份会一起保存 `current-match.json`。
- 回滚备份如果带 `current-match.json`，会一起恢复主预测场次。
- [赛事数据导入格式](赛事数据导入格式.md) 已补充 `currentMatch` 规则。

### 验证结果

- 新增导入测试：payload 提供 `currentMatch` 时会写入新主预测场次，并在备份中保留旧主预测场次。
- 新增导入测试：旧主预测比赛不在新赛程中时拒绝导入，且不写入 teams。
- 增强回滚测试：备份中的 `current-match.json` 会被恢复，回滚前的 current-match 会进入新备份。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，71 个测试。
- `npm run build` 通过。

### 当前判断

真实赛程导入的安全闭环从 teams/fixtures 扩展到了首页主预测入口。接下来整理官方赛程时必须在导入 JSON 中一并指定 `currentMatch`。

## 迭代记录：模型状态缺口口径校准

日期：2026-06-14

### 本次目标

让 `/api/model-status` 的已知缺口准确反映当前能力，避免继续显示过时状态。

### 已完成内容

- 数据缺口改为“官方可核验赛程、真实分组和 48 队名单尚未替换当前样例数据”。
- 新闻缺口改为“新闻 Feed 导入和来源校验已就绪，但真实 Feed 列表与外部定时调度尚未接入”。

### 验证结果

- 更新 API 测试，校验 `knownGaps` 包含“官方可核验赛程”和“真实 Feed”。
- `test_api.py` 20 个测试通过。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，71 个测试。
- `npm run build` 通过。

### 当前判断

系统自检口径已经跟当前实现同步。下一步仍是找到可靠官方赛程数据源，不能用存在冲突的第三方赛程直接覆盖数据。

## 迭代记录：赛事数据来源证明落盘

日期：2026-06-14

### 本次目标

让赛事数据导入不仅写入 teams/fixtures，也能保留可交接的数据来源证明，避免后续无法判断当前预测基于什么数据。

### 已完成内容

- 新增 `backend/data_files/tournament-provenance.json`，当前标记为 `local-sample`。
- 赛事导入会把 payload 里的 `source`、`retrievedAt`、`sourceUrl` 写入 `tournament-provenance.json`。
- 赛事备份和回滚会同步处理 `tournament-provenance.json`。
- 运行态 `DATASET_META` 和 `/api/model-status` 会暴露 `tournamentSource`。
- [赛事数据导入格式](赛事数据导入格式.md) 已补充 provenance 字段和恢复规则。

### 验证结果

- 新增导入测试：导入后会写入 `tournament-provenance.json`，并在备份中保留旧 provenance。
- 新增 API 测试：临时数据目录的 provenance 会出现在 `/api/model-status.dataset.tournamentSource`。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，72 个测试。
- `npm run build` 通过。

### 当前判断

赛事数据现在具备来源证明闭环。真实官方数据导入时必须提供可核验 `sourceUrl`，否则客户交接时无法解释数据边界。

## 迭代记录：当前比赛上下文影响进入五大盘面

日期：2026-06-14

### 本次目标

让旅行、城市转场和恢复时间不只影响概率和分析文字，也能反映到用户可见的五大盘面修正值里。

### 已完成内容

- 后端 `modelMeta` 新增 `fixtureContextImpacts`。
- `fixtureContextImpacts` 会把当前比赛的短休、城市转场等上下文影响映射到 `path` 和 `squad`。
- 前端五大盘面会把新闻事件影响 `factorImpacts` 和当前比赛上下文影响 `fixtureContextImpacts` 相加显示。

### 验证结果

- 新增模型测试：主预测响应会返回巴西短休/转场造成的 `fixtureContextImpacts.brazil.path/squad < 0`。
- `npm run validate:data` 通过。
- `npm run test:model` 通过，73 个测试。
- `npm run build` 通过。
- 浏览器检查通过：通过临时注入 `fixtureContextImpacts`，首页五大盘面在 1280px 和 390px 下都显示路径盘、人员盘 `-1.5`，无错误遮罩和横向溢出。

### 当前判断

动态因子解释现在贯穿“概率、AI 分析、五大盘面”。下一步仍需要真实城市、场馆和开赛时间数据，才能让上下文因子在正式赛程中大规模生效。
